# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/

# Virtual environments
.venv/
venv/
env/

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp
*.bak

# Model weights — multi-GB, fetched separately via scripts
checkpoints/

# Intermediate outputs (generated images, PT reference tensors for byte-diff)
out/

# Nested git directories under refs/ (upstream PT repos) — keep their files,
# drop their git history (`refs/Lance/.git/pack/*.pack` alone is ~99MB).
refs/**/.git/
refs/**/.git
